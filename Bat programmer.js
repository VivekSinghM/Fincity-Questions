const findNumOfStepsRequired = (patient,steps) => patient*steps;
// why this because of Example: findNumOfStepsRequired(3, 10) returns 30
console.log(findNumOfStepsRequired(3, 10));
//issue rased in git